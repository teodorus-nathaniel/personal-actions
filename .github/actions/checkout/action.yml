name: Checkout Holdex Github Actions
description: Checkout Holdex Github Actions repository with provided ref

inputs:
  ref:
    description: Ref to checkout
    required: true

runs:
  using: composite
  steps:
    - name: Checkout Holdex Github Actions
      uses: actions/checkout@v4
      with:
        repository: holdex/github-actions
        ref: ${{ inputs.ref }}
        path: .holdex-actions
