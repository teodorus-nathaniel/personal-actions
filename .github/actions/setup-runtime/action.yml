name: Setup Runtime
description: Setup Bun or pnpm runtime for checks

inputs:
  setup-runtime:
    description: Setup package manager runtime in this action
    required: false
    default: "true"
  package-manager:
    description: Package manager to use (bun or pnpm)
    required: false
    default: "bun"

runs:
  using: composite
  steps:
    - name: Validate package manager
      if: inputs.setup-runtime == 'true'
      shell: bash
      run: |
        if [ "${{ inputs.package-manager }}" != "bun" ] && [ "${{ inputs.package-manager }}" != "pnpm" ]; then
          echo "::error::Invalid package-manager '${{ inputs.package-manager }}'. Supported values: bun, pnpm."
          exit 1
        fi

    - name: Setup Bun
      if: inputs.setup-runtime == 'true' && inputs.package-manager == 'bun'
      uses: oven-sh/setup-bun@v2
      with:
        bun-version: latest

    - name: Setup Node.js
      if: inputs.setup-runtime == 'true' && inputs.package-manager == 'pnpm'
      uses: actions/setup-node@v4
      with:
        node-version: 22

    - name: Setup pnpm
      if: inputs.setup-runtime == 'true' && inputs.package-manager == 'pnpm'
      uses: pnpm/action-setup@v4
